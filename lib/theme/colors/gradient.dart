import 'package:flutter/material.dart';

final defaultGradient = LinearGradient(colors: [
  const Color(0xff0EFFFF).withOpacity(0.58),
  const Color(0xff124DE3).withOpacity(0.58)
], begin: Alignment.topCenter, end: Alignment.bottomCenter);

final unenableGradient = LinearGradient(colors: [
  const Color(0xff949494).withOpacity(0.58),
  const Color(0xff505050).withOpacity(0.58)
], begin: Alignment.topCenter, end: Alignment.bottomCenter);
